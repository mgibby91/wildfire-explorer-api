// TODO: scheduled job to refresh NASA FIRMS active fire data
// Run on an interval (e.g. every 10 minutes) and upsert into PostGIS
export const startFirmsPoller = (): void => {
  // implement polling logic here
};
